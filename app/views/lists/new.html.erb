<h1>Create a New List</h1>

<%= form_for(@list) do |f| %>
  <ul>
    <% @list.errors.full_messages.each do |error| %>
      <li><%= error %></li>
    <% end %>
  </ul>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.submit %>
<% end %>
