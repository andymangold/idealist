<h1><%= @list.title %></h1>

<%= link_to "Delete #{@list.title}", list_path(@list), method: :delete, data: {confirm: "Are you sure you want to delete #{@list.title}?"} %>
<%= link_to "Edit #{@list.title}", edit_list_path(@list) %>
<%= link_to "All Lists", lists_path %>
